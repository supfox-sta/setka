{extends "../@layout.xml"}
{block title}Установка Сетки{/block}

{block header}Установка Сетки{/block}

{block content}
    <style nonce="{$nonce}">
        .install-guide {
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            color: #333;
            line-height: 1.6;
        }

        .step-card {
            background: white;
            border: 2px solid #e0e0e0;
            border-radius: 12px;
            padding: 20px;
            margin: 15px 0;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            transition: all 0.3s ease;
        }

        .step-card:hover {
            border-color: #4CAF50;
            box-shadow: 0 4px 16px rgba(0,0,0,0.15);
        }

        .step-header {
            display: flex;
            align-items: center;
            margin-bottom: 15px;
        }

        .step-number {
            background: linear-gradient(135deg, #4CAF50, #45a049);
            color: white;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 18px;
            margin-right: 15px;
            flex-shrink: 0;
        }

        .step-title {
            font-size: 20px;
            font-weight: bold;
            color: #333;
            margin: 0;
        }

        .step-description {
            color: #666;
            font-style: italic;
            margin: 5px 0 15px 0;
        }

        .command-block {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 15px;
            margin: 10px 0;
            position: relative;
        }

        .command-block::before {
            content: "$";
            position: absolute;
            left: 10px;
            top: 12px;
            color: #6c757d;
            font-family: monospace;
            font-weight: bold;
        }

        .command-block code {
            display: block;
            padding-left: 20px;
            font-family: 'Courier New', monospace;
            font-size: 14px;
            line-height: 1.4;
            color: #495057;
            white-space: pre-wrap;
            word-break: break-word;
        }

        .warning-box {
            background: linear-gradient(135deg, #fff3cd, #ffeaa7);
            border: 1px solid #f39c12;
            border-radius: 8px;
            padding: 15px;
            margin: 15px 0;
            display: flex;
            align-items: flex-start;
        }

        .warning-box::before {
            content: "⚠️";
            font-size: 24px;
            margin-right: 10px;
            flex-shrink: 0;
        }

        .success-box {
            background: linear-gradient(135deg, #d4edda, #c3e6cb);
            border: 1px solid #28a745;
            border-radius: 8px;
            padding: 15px;
            margin: 15px 0;
            display: flex;
            align-items: flex-start;
        }

        .success-box::before {
            content: "✅";
            font-size: 24px;
            margin-right: 10px;
            flex-shrink: 0;
        }

        .config-block {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 15px;
            margin: 15px 0;
            font-family: 'Courier New', monospace;
            font-size: 13px;
            white-space: pre-wrap;
            word-break: break-word;
            overflow-x: auto;
            position: relative;
        }

        .config-block::before {
            content: "📄";
            position: absolute;
            right: 10px;
            top: 10px;
            opacity: 0.5;
        }

        .tip-box {
            background: linear-gradient(135deg, #e3f2fd, #bbdefb);
            border: 1px solid #2196F3;
            border-radius: 8px;
            padding: 15px;
            margin: 15px 0;
            display: flex;
            align-items: flex-start;
        }

        .tip-box::before {
            content: "💡";
            font-size: 24px;
            margin-right: 10px;
            flex-shrink: 0;
        }
    </style>

    <div class="install-guide">
        <p style="text-align: center; color: #666; font-size: 16px; margin-bottom: 30px;">
            Добро пожаловать! Следуйте этому простому пошаговому руководству, и ваша социальная сеть будет готова через 15-20 минут.
        </p>

        <!-- Step 1: Server Preparation -->
        <div class="step-card" id="step1">
            <div class="step-header">
                <div class="step-number">1</div>
                <div>
                    <h3 class="step-title">🛠️ Подготовка сервера</h3>
                    <p class="step-description">Проверяем и настраиваем базовую безопасность сервера</p>
                </div>
            </div>

            <div class="warning-box">
                <div>
                    <strong>Важно!</strong> Выполняйте команды по порядку. Если что-то пойдет не так - не паникуйте, мы поможем в поддержке.
                </div>
            </div>

            <p><strong>Шаг 1.1:</strong> Проверьте статус безопасности SELinux:</p>
            <div class="command-block">
                <code>sestatus</code>
            </div>

            <p><strong>Шаг 1.2:</strong> Если SELinux включен (enabled), отключите его:</p>
            <div class="tip-box">
                <div>
                    <strong>Как отключить:</strong> Откройте файл <code>/etc/sysconfig/selinux</code> и измените <code>SELinux=enforcing</code> на <code>SELinux=disabled</code>, затем выполните <code>reboot</code>
                </div>
            </div>
        </div>

        <!-- Step 2: Dependencies -->
        <div class="step-card" id="step2">
            <div class="step-header">
                <div class="step-number">2</div>
                <div>
                    <h3 class="step-title">📦 Установка программ</h3>
                    <p class="step-description">Устанавливаем PHP, Node.js и все необходимые компоненты</p>
                </div>
            </div>

            <p><strong>Шаг 2.1:</strong> Добавьте репозитории для свежих версий программ:</p>
            <div class="command-block">
                <code>dnf -y install epel-release</code>
            </div>
            <div class="command-block">
                <code>dnf -y install https://rpms.remirepo.net/enterprise/remi-release-8.rpm</code>
            </div>

            <p><strong>Шаг 2.2:</strong> Включите PHP 8.2 и Node.js:</p>
            <div class="command-block">
                <code>dnf -y module enable php:remi-8.2</code>
            </div>
            <div class="command-block">
                <code>dnf -y module enable nodejs:14</code>
            </div>

            <p><strong>Шаг 2.3:</strong> Установите все необходимые программы:</p>
            <div class="command-block">
                <code>dnf -y install php php-cli php-common unzip php-zip php-yaml php-gd php-pdo_mysql nodejs git</code>
            </div>

            <p><strong>Шаг 2.4:</strong> Установите инструменты разработчика:</p>
            <div class="command-block">
                <code>npm i -g yarn</code>
            </div>
            <div class="command-block">
                <code>php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"</code>
            </div>
            <div class="command-block">
                <code>php composer-setup.php --filename=composer2 --install-dir=/bin --snapshot</code>
            </div>
            <div class="command-block">
                <code>rm composer-setup.php</code>
            </div>
        </div>

        <!-- Step 3: Database -->
        <div class="step-card" id="step3">
            <div class="step-header">
                <div class="step-number">3</div>
                <div>
                    <h3 class="step-title">🗄️ Настройка базы данных</h3>
                    <p class="step-description">Устанавливаем и настраиваем MySQL для хранения данных</p>
                </div>
            </div>

            <p><strong>Шаг 3.1:</strong> Установите MySQL сервер:</p>
            <div class="command-block">
                <code>dnf -y install https://repo.percona.com/yum/percona-release-latest.noarch.rpm</code>
            </div>
            <div class="command-block">
                <code>percona-release setup -y ps80</code>
            </div>
            <div class="command-block">
                <code>dnf -y install percona-server-server percona-toolkit</code>
            </div>
            <div class="command-block">
                <code>systemctl start mysql</code>
            </div>

            <p><strong>Шаг 3.2:</strong> Найдите временный пароль MySQL:</p>
            <div class="command-block">
                <code>cat /var/log/mysqld.log | grep password</code>
            </div>

            <p><strong>Шаг 3.3:</strong> Настройте безопасность MySQL:</p>
            <div class="command-block">
                <code>mysql_secure_installation</code>
            </div>

            <div class="warning-box">
                <div>
                    <strong>Запомните пароль!</strong> Он понадобится для настройки Сетки. При настройке отвечайте: n, y, y, y, y
                </div>
            </div>
        </div>

        <!-- Step 4: Install Setka -->
        <div class="step-card" id="step4">
            <div class="step-header">
                <div class="step-number">4</div>
                <div>
                    <h3 class="step-title">⚙️ Установка Сетки</h3>
                    <p class="step-description">Скачиваем и устанавливаем социальную сеть</p>
                </div>
            </div>

            <p><strong>Шаг 4.1:</strong> Установите Chandler (платформу):</p>
            <div class="command-block">
                <code>cd /opt</code>
            </div>
            <div class="command-block">
                <code>git clone https://github.com/openvk/chandler.git</code>
            </div>
            <div class="command-block">
                <code>cd chandler/</code>
            </div>
            <div class="command-block">
                <code>composer2 install</code>
            </div>

            <p><strong>Шаг 4.2:</strong> Создайте секретный ключ:</p>
            <div class="command-block">
                <code>cat /dev/random | tr -dc 'a-z0-9' | fold -w 128 | head -n 1</code>
            </div>

            <p><strong>Шаг 4.3:</strong> Настройте Chandler. Создайте файл <code>chandler.yml</code>:</p>
            <div class="config-block">chandler:
    debug: true
    websiteUrl: null
    rootApp: "setka"

    preferences:
        appendExtension: "xhtml"
        adminUrl: "/chandlerd"
        exposeChandler: true

    database:
        dsn: "mysql:unix_socket=/var/lib/mysql/mysql.sock;dbname=setka_main"
        user: "root"
        password: "ВАШ_ПАРОЛЬ_БД"

    security:
        secret: "ВАШ_СЕКРЕТНЫЙ_КЛЮЧ"
        csrfProtection: "permissive"
        sessionDuration: 14</div>

            <p><strong>Шаг 4.4:</strong> Установите компоненты:</p>
            <div class="command-block">
                <code>cd extensions/available/</code>
            </div>
            <div class="command-block">
                <code>git clone https://github.com/openvk/commitcaptcha.git</code>
            </div>
            <div class="command-block">
                <code>cd commitcaptcha/</code>
            </div>
            <div class="command-block">
                <code>composer2 install</code>
            </div>

            <p><strong>Шаг 4.5:</strong> Скачайте Сетку:</p>
            <div class="command-block">
                <code>cd ..</code>
            </div>
            <div class="command-block">
                <code>git clone https://github.com/supfox-sta/setka.git</code>
            </div>
            <div class="command-block">
                <code>cd setka/</code>
            </div>
            <div class="command-block">
                <code>composer2 install</code>
            </div>
            <div class="command-block">
                <code>cd Web/static/js</code>
            </div>
            <div class="command-block">
                <code>yarn install</code>
            </div>
        </div>

        <!-- Step 5: Configuration -->
        <div class="step-card" id="step5">
            <div class="step-header">
                <div class="step-number">5</div>
                <div>
                    <h3 class="step-title">🔧 Настройка конфигурации</h3>
                    <p class="step-description">Настраиваем файлы конфигурации</p>
                </div>
            </div>

            <p><strong>Шаг 5.1:</strong> Создайте файл <code>openvk.yml</code> в папке setka:</p>
            <div class="config-block">openvk:
    debug: true
    appearance:
        name: "Сетка | Социальная сеть ностальгии"
        motd: "Социальная сеть ностальгии по старому ВК"
    preferences:
        femaleGenderPriority: true
        uploads:
            disableLargeUploads: false
            mode: "basic"
        shortcodes:
            forbiddenNames:
                - "index.php"
        security:
            requireEmail: false
            requirePhone: false
            forcePhoneVerification: false
            forceEmailVerification: false
            enableSu: true
        support:
            supportName: "Модератор"
            adminAccount: 1
        messages:
            strict: false
        wall:
            postSizes:
                maxSize: 60000
                processingLimit: 3000
                emojiProcessingLimit: 1000
        adPoster:
            enable: false
        bellsAndWhistles:
            testLabel: false
    credentials:
        smsc:
            enable: false
        eventDB:
            enable: true
            database:
                dsn: "mysql:unix_socket=/var/lib/mysql/mysql.sock;dbname=setka_events"
                user: "root"
                password: "ВАШ_ПАРОЛЬ_БД"</div>

            <p><strong>Шаг 5.2:</strong> Подключите компоненты:</p>
            <div class="command-block">
                <code>ln -s /opt/chandler/extensions/available/commitcaptcha/ /opt/chandler/extensions/enabled/commitcaptcha</code>
            </div>
            <div class="command-block">
                <code>ln -s /opt/chandler/extensions/available/setka/ /opt/chandler/extensions/enabled/setka</code>
            </div>
        </div>

        <!-- Step 6: Database Setup -->
        <div class="step-card" id="step6">
            <div class="step-header">
                <div class="step-number">6</div>
                <div>
                    <h3 class="step-title">💾 Настройка базы данных</h3>
                    <p class="step-description">Создаем базы данных и загружаем начальные данные</p>
                </div>
            </div>

            <p><strong>Шаг 6.1:</strong> Создайте базы данных:</p>
            <div class="command-block">
                <code>mysql -p -e "CREATE DATABASE setka_main;"</code>
            </div>
            <div class="command-block">
                <code>mysql -p -e "CREATE DATABASE setka_events;"</code>
            </div>

            <p><strong>Шаг 6.2:</strong> Загрузите структуру базы данных:</p>
            <div class="command-block">
                <code>mysql -p setka_main < /opt/chandler/install/init-db.sql</code>
            </div>
            <div class="command-block">
                <code>mysql -p setka_main < /opt/chandler/extensions/available/setka/install/init-static-db.sql</code>
            </div>
            <div class="command-block">
                <code>mysql -p setka_events < /opt/chandler/extensions/available/setka/install/init-event-db.sql</code>
            </div>
        </div>

        <!-- Step 7: Web Server -->
        <div class="step-card" id="step7">
            <div class="step-header">
                <div class="step-number">7</div>
                <div>
                    <h3 class="step-title">🌐 Настройка веб-сервера</h3>
                    <p class="step-description">Настраиваем Apache для работы с Сеткой</p>
                </div>
            </div>

            <p><strong>Шаг 7.1:</strong> Дайте права Apache:</p>
            <div class="command-block">
                <code>chown -R apache: /opt/chandler/</code>
            </div>

            <p><strong>Шаг 7.2:</strong> Создайте конфигурацию Apache <code>/etc/httpd/conf.d/10-setka.conf</code>:</p>
            <div class="config-block"><VirtualHost *:80>
    ServerName setka.local
    DocumentRoot /opt/chandler/htdocs

    <Directory /opt/chandler/htdocs>
        AllowOverride All
        Require all granted
    </Directory>

    ErrorLog /var/log/setka/error.log
    CustomLog /var/log/setka/access.log combinedio
</VirtualHost></div>

            <p><strong>Шаг 7.3:</strong> Включите необходимые модули:</p>
            <div class="command-block">
                <code>echo "LoadModule rewrite_module modules/mod_rewrite.so" > /etc/httpd/conf.modules.d/02-rewrite.conf</code>
            </div>

            <p><strong>Шаг 7.4:</strong> Создайте папку для логов:</p>
            <div class="command-block">
                <code>mkdir /var/log/setka && chown apache: /var/log/setka/</code>
            </div>

            <p><strong>Шаг 7.5:</strong> Откройте порт в firewall:</p>
            <div class="command-block">
                <code>firewall-cmd --permanent --add-port=80/tcp && firewall-cmd --reload</code>
            </div>

            <p><strong>Шаг 7.6:</strong> Запустите Apache:</p>
            <div class="command-block">
                <code>systemctl start httpd && systemctl enable httpd</code>
            </div>
        </div>

        <!-- Success -->
        <div class="step-card" id="step8">
            <div class="step-header">
                <div class="step-number">🎉</div>
                <div>
                    <h3 class="step-title">Готово!</h3>
                    <p class="step-description">Сетка установлена и готова к работе</p>
                </div>
            </div>

            <div class="success-box">
                <div>
                    <strong>Поздравляем!</strong> Ваша социальная сеть Сетка успешно установлена и работает!
                </div>
            </div>

            <div style="text-align: center; margin: 20px 0;">
                <h4>🚪 Вход в админку:</h4>
                <p><strong>Адрес:</strong> http://ваш-сервер/</p>
                <p><strong>Логин:</strong> admin@localhost.localdomain6</p>
                <p><strong>Пароль:</strong> admin</p>
            </div>

            <div class="warning-box">
                <div>
                    <strong>Важно!</strong> Сразу после входа измените пароль администратора в настройках безопасности.
                </div>
            </div>
        </div>

        <!-- FAQ -->
        <div class="step-card">
            <div class="step-header">
                <div class="step-number">❓</div>
                <div>
                    <h3 class="step-title">Часто задаваемые вопросы</h3>
                    <p class="step-description">Ответы на популярные вопросы</p>
                </div>
            </div>

            <div style="margin-top: 15px;">
                <p><strong>Q: Установка прервалась с ошибкой. Что делать?</strong></p>
                <p>A: Проверьте логи команд. Часто проблема в правах доступа или отсутствии интернета.</p>

                <p><strong>Q: Сайт не открывается после установки.</strong></p>
                <p>A: Проверьте, запущен ли Apache (<code>systemctl status httpd</code>) и открыт ли порт 80.</p>

                <p><strong>Q: Не получается войти в админку.</strong></p>
                <p>A: Убедитесь, что ввели правильный логин и пароль. Проверьте логи Apache.</p>

                <p><strong>Q: Где найти помощь?</strong></p>
                <p>A: Создайте issue в <a href="https://github.com/supfox-sta/setka/issues" target="_blank">репозитории на GitHub</a></p>
            </div>
        </div>
    </div>
{/block}
